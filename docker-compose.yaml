services:

  mysql:
    image: mysql
    container_name: db
    platform: linux/amd64
    volumes:
      - .docker/mysql:/var/lib/mysql
    ports:
      - "3306:3306"
    environment:
      - MYSQL_DATABASE=wallet
      - MYSQL_ROOT_PASSWORD=root